using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerSlot : MonoBehaviour
{
    public bool isPlayerSlotfull;
    // Start is called before the first frame update
    
}
